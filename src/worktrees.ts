import {spawnSync} from "node:child_process";import path from "node:path";import fs from "node:fs";import {config} from "./config.js";function git(cwd:string,args:string[]){const r=spawnSync("git",args,{cwd,encoding:"utf8"});if(r.status!==0)throw new Error(r.stderr||"git failed");return r.stdout.trim()}export function ensureWorktree(repoDir:string,id:string,branch:string){const root=path.join(config.dataDir,"worktrees");fs.mkdirSync(root,{recursive:true});const target=path.join(root,id);if(fs.existsSync(target))return target;git(repoDir,["worktree","add","-b",branch,target,config.defaultBranch]);return target}