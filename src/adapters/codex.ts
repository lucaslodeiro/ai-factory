import {config} from "../config.js";import {ExecutionManager} from "../execution-manager.js";import type {AgentAdapter,AgentRunRequest} from "./agent.js";export class CodexAdapter implements AgentAdapter{constructor(private executions:ExecutionManager){}run(r:AgentRunRequest){return this.executions.run(r.workItemId,r.role,config.codexCommand,["exec","--full-auto",r.instructions],r.cwd)}}