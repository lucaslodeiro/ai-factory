import test from "node:test"; import assert from "node:assert/strict"; import {canTransition,assertTransition} from "../src/state-machine.js"; test("happy path",()=>{assert.equal(canTransition("NEW","SPEC"),true);assert.equal(canTransition("DEVELOPMENT","QA"),true);assert.equal(canTransition("QA","REVIEW"),true);assert.equal(canTransition("REVIEW","READY_TO_MERGE"),true)}); test("invalid transition",()=>assert.throws(()=>assertTransition("NEW","READY_TO_MERGE")));